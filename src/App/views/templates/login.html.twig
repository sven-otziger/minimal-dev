<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Login</title>
    <style>

        html {
            font-family: sans-serif;
        }

        body {
            padding: 0;
            margin: 5rem 2rem;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .background {
            background-color: rgba(13, 110, 253, 0.25);
            width: 60%;
            line-height: 2rem;
            padding: 2rem;
        }

        form.login-form label {
            display: inline-block;
            width: 100px;
        }

        form.login-form input[type="text"],
        form.login-form input[type="password"] {
            width: 200px;
        }

        div.submit {
            text-align: right;
        }

        form.signup-form {
            font-size: .8rem;
        }

    </style>
</head>
<body>
<div class="background">
    <form action="/login" method="post" class="login-form">

        <div>
            <label for="username">Username</label>
            <input type="text" id="username" name="username">
        </div>
        <div>
            <label for="password">Password</label>
            <input type="password" id="password" name="password">
        </div>
        <div class="submit">
            <input type="submit" value="Log in">
        </div>

    </form>

    <div>
        <form action="/create-user-form" method="post" class="signup-form">
            <label for="submit-new-user">Not yet having an account. Create one here:</label>
            <input type="submit" id="submit-new-user" value="Create a new account">
        </form>
    </div>
</div>


<div>
    <p>{{ message }}</p>
</div>

</body>
</html>